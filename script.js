const sections=[...document.querySelectorAll('[data-section]')],wheelLinks=[...document.querySelectorAll('.section-wheel a')],marker=document.querySelector('.wheel-track span');
const observer=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){const i=sections.indexOf(e.target);wheelLinks.forEach(l=>l.classList.toggle('active',l.hash===`#${e.target.id}`));if(marker)marker.style.transform=`translate(-1px, ${i*100}%)`;}})},{rootMargin:'-30% 0px -55%'});sections.forEach(s=>observer.observe(s));
const playground=document.querySelector('#playground'),note=document.querySelector('.play-note');if(playground&&matchMedia('(pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion: reduce)').matches){playground.addEventListener('pointermove',e=>{const r=playground.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;playground.querySelector('.orbit-one').style.transform=`translate(${x*13}px,${y*13}px)`;playground.querySelector('.orbit-two').style.transform=`rotate(36deg) translate(${x*-10}px,${y*-10}px)`})}document.querySelectorAll('.interest').forEach(item=>item.addEventListener('click',()=>{note.textContent=item.dataset.note;setTimeout(()=>note.innerHTML='Move around the map <span>↗</span>',3200)}));
